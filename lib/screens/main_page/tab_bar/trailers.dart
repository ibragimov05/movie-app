import 'package:flutter/cupertino.dart';

class Trailers extends StatelessWidget {
  const Trailers({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset('assets/images/page_1/avatar.png'),
      ],
    );
  }
}
